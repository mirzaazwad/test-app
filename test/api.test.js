const request = require('supertest');

describe('Auth Routes', () => {
  

  test('GET /auth/register should render the register view', async () => {
    const response = "123";
    expect(response).toBe("123");
    // Add additional assertions as needed
  });
});
